Текущая тема: Урок 1.3. Компоненты и элементы.
Мы начнем с фундамента: разберемся, чем компонент отличается от элемента, настроим правильную структуру папок и напишем наши первые функциональные компоненты, используя JSX.

---

📘 Теория

1. Элементы vs Компоненты
   • Элемент — это «базовая единица», простой объект, описывающий, что мы хотим увидеть на экране (например, <div>...</div>). Это строительные блоки.
   • Компонент — это независимый, переиспользуемый кусок кода (обычно функция), который возвращает элементы (JSX-разметку).
2. Структура проекта Хорошим тоном считается держать компоненты в папке components. Для каждого компонента (если у него есть свои стили или доп. файлы) лучше создавать отдельную подпапку (например, components/MyName/).
3. Формат файлов Мы будем использовать расширение .jsx. Это подсказывает редактору кода и сборщику (Vite), что внутри есть HTML-подобный синтаксис, и включает полезные подсказки.
4. Экспорт и Импорт Существует два способа подключить компонент в другой файл:
   • Именованный (Named): export function Name... -> import { Name } from .... Удобно, когда в файле несколько сущностей.
   • По умолчанию (Default): export default Name -> import Name from ... (без скобок). Считается стандартом для файлов с одним компонентом.

---

🛠 Практика
В текущем состоянии твой проект пуст (App.jsx возвращает <>). Давай наполним его жизнью.
Задача 1: Создание структуры и первого компонента
Мы создадим компонент MyName (как в уроке), используя именованный экспорт, чтобы потренировать внимательность к фигурным скобкам.
Шаг 1: В корне проекта создай папку components. Шаг 2: Внутри components создай папку MyName. Шаг 3: В папке MyName создай файл MyName.jsx.
Файл: src/components/MyName/MyName.jsx (Вставь код ниже, дополнив пропущенные части)
// Мы используем именованный экспорт (обрати внимание на слово export перед function) [7]
export function MyName() {
// Компонент должен возвращать JSX разметку [2]
return (
<div className="my-name-card">
{/_ Напиши внутри заголовок h2 с текстом "Привет, я [Твое Имя]" _/}
{/_ Напиши параграф p с текстом "Это мой первый компонент!" _/}
</div>
);
}
Шаг 4: В той же папке создай файл стилей MyName.css.
Файл: src/components/MyName/MyName.css (Скопируй полностью)
.my-name-card {
border: 2px solid #646cff;
border-radius: 8px;
padding: 20px;
background-color: #f9f9f9;
text-align: center;
margin: 20px 0;
}

.my-name-card h2 {
color: #333;
margin-bottom: 10px;
}

.my-name-card p {
color: #666;
font-style: italic;
}
Шаг 5: Не забудь импортировать этот CSS файл внутри MyName.jsx, иначе стили не применятся. Добавь import './MyName.css'; в начало файла MyName.jsx.

---

Задача 2: Подключение компонента в App
Теперь нужно отрисовать созданный компонент внутри главного приложения.
Файл: src/App.jsx (Допиши импорт и вызов компонента)
// 1. Импортируй компонент MyName.
// ВАЖНО: Так как экспорт был именованный, используй фигурные скобки { } [7]
// Путь будет: './components/MyName/MyName'

// import { ... } from ...

function App() {
return (
<>
<h1>Мое приложение</h1>
{/_ 2. Вызови компонент MyName здесь как тег (не забудь закрыть его) [2] _/}

    </>

);
}

export default App;
👀 Результат в браузере: Ты должен увидеть заголовок "Мое приложение" и под ним красивую карточку с твоим именем в рамке.

---

Задача 3: Компонент с экспортом "по умолчанию"
Теперь создадим компонент Footer (подвал сайта), но используем export default, чтобы почувствовать разницу.
Шаг 1: Создай папку components/Footer. Шаг 2: Создай файл components/Footer/Footer.jsx.
Файл: src/components/Footer/Footer.jsx
import './Footer.css';

function Footer() {
return (
<footer className="site-footer">
<p>React Course 2026. Все права защищены.</p>
</footer>
);
}

// Используем экспорт по умолчанию. Это упрощает импорт в будущем [8].
// Напиши команду экспорта ниже:
// export ...
Шаг 3: Создай файл components/Footer/Footer.css.
Файл: src/components/Footer/Footer.css (Скопируй полностью)
.site-footer {
margin-top: 40px;
padding: 10px;
background-color: #333;
color: white;
text-align: center;
font-size: 0.8rem;
}
Шаг 4: Подключи Footer в App.jsx.
Файл: src/App.jsx (Добавь изменения)
// ... старые импорты

// Импорт компонента с default export (без фигурных скобок!) [8]
// import Footer from './components/Footer/Footer';

function App() {
return (
<>
<h1>Мое приложение</h1>
{/_ Компонент MyName _/}

      {/* Вызови компонент Footer ниже */}

    </>

);
}

export default App;
👀 Результат в браузере: Внизу страницы появится темная полоса подвала с белым текстом.
