Источники прочитаны. Я готов приступить к работе в роли твоего ментора по React.

Опираясь на предоставленный план курса и транскрипцию видеоурока, мы находимся на **Уроке 1.5**.

Ниже представлен теоретический разбор и практические задачи для твоего проекта `my-app-2026`.

---

### 🎓 Текущая тема: Урок 1.5. JSX: синтаксис, вставка выражений {}, фрагменты <>

Мы работаем с чистым проектом. Твоя задача — освоить правила написания разметки внутри JavaScript.

#### 1. Теория: Что такое JSX?

- **Это не HTML.** JSX — это синтаксический сахар над JavaScript. Всё, что выглядит как теги, на самом деле превращается в вызовы функций `React.createElement`.
- **Вставка переменных:** Чтобы вставить JavaScript-выражение (переменную, строку, число) в разметку, используются фигурные скобки `{}`. Например: `<h1>{name}</h1>`.
- **Правило одного родителя:** Компонент не может вернуть несколько соседних элементов. Их нужно обернуть в `<div>` или, что лучше, в **Фрагмент** `<> ... </>` (пустой тег), чтобы не создавать лишних узлов в DOM.

#### 2. Важные отличия от HTML

1.  **Закрытие тегов:** В JSX **все** теги должны быть закрыты. Теги без содержимого (как `img`, `input`, `br`) обязательно пишутся с косой чертой в конце: `<img />`, `<br />`.
2.  **Атрибуты:** Используется camelCase.
    - Вместо `class` пишем **`className`**.
    - Вместо `for` (в лейблах) пишем **`htmlFor`**.
3.  **Стили (Inline Styles):** Инлайн-стили задаются не строкой, а **объектом**. Свойства пишутся в camelCase (например, `fontSize` вместо `font-size`).
    - Пример: `style={{ color: 'blue', fontSize: 20 }}`.

#### 3. Условный рендеринг и безопасность

- **Логическое И (&&):** Позволяет показать элемент только если условие истинно. Если условие `false`, `null` или `undefined`, React ничего не выведет,.
- **Безопасность:** JSX автоматически экранирует строки, защищая от XSS-атак. Если нужно вставить "сырой" HTML (что опасно), используется атрибут `dangerouslySetInnerHTML`,.

---

### 🛠 Практические задачи

Мы работаем в файле `src/App.jsx`. Сейчас он возвращает пустой фрагмент `<></>`.

#### Задача 1: Профиль пользователя и стили

**Цель:** Научиться вставлять переменные и использовать объектный стиль.

1.  Внутри функции `App` (до `return`) создай константу `userData` с полями `name` (строка) и `avatar` (ссылка на любую картинку).
2.  Создай объект стилей `avatarStyle`, задав ему ширину `100px` и скругление углов `50%` (помни про camelCase!).
3.  В `return` верни разметку с именем пользователя и картинкой, применив к ней созданные стили.

**Файл: `src/App.jsx`**

```javascript
import "./App.css"; // Оставляем импорт стилей

function App() {
  // 1. Создай объект userData с именем и ссылкой на фото
  // const userData = ...

  // 2. Создай объект стилей для картинки (borderRadius, width)
  // const avatarStyle = ...

  return (
    <>
      {/* 3. Выведи h1 с именем пользователя, используя {} */}

      {/* 4. Выведи тег img. 
          - src возьми из userData
          - style возьми из avatarStyle
          - НЕ ЗАБУДЬ закрыть тег!
          - alt укажи текстом
      */}
    </>
  );
}

export default App;
```

**Что должно получиться:** В браузере должно отобразиться имя и круглая аватарка.

---

#### Задача 2: Работа с атрибутами и классами

**Цель:** Использовать правильные имена атрибутов (`className`, `htmlFor`) и закрывать одиночные теги.

В этом задании мы добавим немного CSS в файл стилей, чтобы проверить работу `className`.

**Файл: `src/App.css` (Замени всё содержимое на этот код)**

```css
.card {
  border: 1px solid #ccc;
  padding: 20px;
  margin-top: 20px;
  border-radius: 8px;
  font-family: sans-serif;
}

.input-field {
  display: block;
  margin-top: 10px;
  padding: 5px;
}
```

**Файл: `src/App.jsx` (Дополни предыдущий код)**
Вставь эту разметку ниже картинки из первой задачи (внутри фрагмента `<>...</>`).

```javascript
      {/* ...код из первой задачи... */}

      <hr /> {/* Обрати внимание, тег hr должен быть закрыт */}

      {/*
        Задание:
        1. Создай div. Вместо class="card" используй правильный атрибут для JSX.
        2. Внутри div добавь label с текстом "Введите email".
           Вместо for="email" используй правильный атрибут.
        3. Добавь input.
           - id="email"
           - type="email"
           - Присвой ему класс input-field (через правильный атрибут)
           - Убедись, что тег input закрыт.
      */}
```

**Что должно получиться:** Под чертой появится карточка с полем ввода, к которой применились стили из CSS файла. Консоль разработчика не должна выдавать ошибок про `class` или незакрытые теги.

---

#### Задача 3: Условный рендеринг (Ternary & &&)

**Цель:** Скрывать или показывать элементы в зависимости от условий.

1.  Создай переменную `isLoggedIn`. Попробуй менять её значение (`true`/`false`).
2.  Используй тернарный оператор или логическое `&&` для вывода сообщения.

**Файл: `src/App.jsx`**

```javascript
// Добавь это в начало компонента
const isLoggedIn = false; // Попробуй поменять на true

return (
  <>
    {/* ...предыдущий код... */}

    <div style={{ marginTop: 20 }}>
      {/* 
          Задание:
          Если isLoggedIn === true, покажи кнопку <button>Выйти</button>
          Если isLoggedIn === false, покажи кнопку <button>Войти</button>
          
          Используй тернарный оператор: условие ? (код) : (код)
        */}

      {/* 
          Дополнительно:
          Если isLoggedIn === true, выведи параграф <p>Добро пожаловать!</p>
          Используй оператор &&
        */}
    </div>
  </>
);
```

**Что должно получиться:**

- При `isLoggedIn = false`: Видна кнопка "Войти".
- При `isLoggedIn = true`: Видна кнопка "Выйти" и текст "Добро пожаловать!".

---

Жду выполнения! Если возникнут ошибки в консоли (например, "Unterminated JSX contents" или "Invalid DOM property"), пиши — разберем.
