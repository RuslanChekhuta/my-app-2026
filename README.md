Привет! Я изучил предоставленные источники. Как твой ментор по React, я вижу, что мы находимся на **Модуле 1**, и наша текущая тема — **Урок 1.13: Условный рендеринг**.

Мы будем работать в проекте `my-app-2026`. Твой `App.jsx` сейчас пуст, и мы наполним его логикой, которая позволяет интерфейсу меняться в зависимости от данных.

Ниже представлена теория и практические задачи, составленные строго по материалам урока- и программе курса.

---

### 🎓 Теория: Условный рендеринг

Условный рендеринг — это инструмент, позволяющий отображать разные элементы интерфейса в зависимости от выполнения определенных условий. Мы разберем три основных подхода.

#### 1. Оператор `if` (и ранний возврат)

Это самый простой способ. Он отлично подходит, когда нужно выполнить проверку и вернуть совершенно разные JSX-структуры или когда логика сложная. Часто используется паттерн "Early Return" (ранний возврат): если условие не выполнено, мы возвращаем `null` или заглушку, прерывая выполнение функции компонента.

> _Пример:_ Если пользователь не авторизован, показываем кнопку входа. Если авторизован — приветствие.

#### 2. Тернарный оператор (`condition ? true : false`)

Компактная запись, идеально подходящая для выбора между двумя вариантами внутри JSX.
Синтаксис: `условие ? (что показать, если true) : (что показать, если false)`.

#### 3. Логический оператор «И» (`&&`)

Используется, когда нужно отобразить элемент **только** если условие истинно, а если ложно — не отображать ничего.
Важный нюанс: если левая часть выражения — это `0` (число), React может отрендерить его как `0` в интерфейсе, так как это не `false`, `null` или `undefined`. Поэтому лучше проверять `length > 0`, а не просто `length`.

---

### 🛠 Практика

**Цель:** Научиться скрывать/показывать элементы и менять их вид в зависимости от пропсов и стейта.

#### Задача 1: Компонент статуса (Тернарный оператор)

Создадим компонент, который показывает, в сети пользователь или нет.

1.  Создай файл `src/components/UserStatus.jsx`.
2.  Используй **тернарный оператор**, чтобы менять текст и CSS-класс.

**CSS (добавь в `src/App.css` — я даю полный код по правилам):**

```css
/* Задача 1 */
.status-badge {
  padding: 8px 16px;
  border-radius: 20px;
  font-weight: bold;
  display: inline-block;
  margin-bottom: 20px;
}

.status-online {
  background-color: #dcfce7;
  color: #166534;
  border: 1px solid #166534;
}

.status-offline {
  background-color: #f3f4f6;
  color: #374151;
  border: 1px solid #9ca3af;
}
```

**JS (Скелет `src/components/UserStatus.jsx`):**

```jsx
// 1. Прими проп isOnline
function UserStatus({ isOnline }) {
  return (
    // 2. В className используй тернарный оператор:
    // если isOnline true -> 'status-badge status-online'
    // иначе -> 'status-badge status-offline'
    <div className={/* Твой код здесь */}>
      {/* 3. Внутри текста тоже тернарный оператор: "В сети" или "Не в сети" */}
      {/* Твой код здесь */}
    </div>
  );
}

export default UserStatus;
```

---

#### Задача 2: Панель уведомлений (Логическое И `&&`)

Создадим компонент, который показывает сообщение, только если есть новые уведомления. В видео-уроке предупреждали о проблеме с нулем, учти это.

1.  Создай файл `src/components/NotificationArea.jsx`.

**CSS (добавь в `src/App.css`):**

```css
/* Задача 2 */
.notification-box {
  background-color: #ffedd5;
  border-left: 4px solid #f97316;
  padding: 10px;
  margin: 10px 0;
  color: #9a3412;
}
```

**JS (Скелет `src/components/NotificationArea.jsx`):**

```jsx
function NotificationArea({ newMessagesCount }) {
  // Вспомни урок: если newMessagesCount равно 0, React может нарисовать "0".
  // Используй сравнение (newMessagesCount > 0), чтобы получить true/false.

  return (
    <div>
      <h3>Центр уведомлений</h3>

      {/* 1. Напиши условие с &&. Если сообщений > 0, покажи div ниже */}
      {
        /* Твой код проверки && */ <div className="notification-box">
          У вас {newMessagesCount} новых сообщений!
        </div>
      }
    </div>
  );
}

export default NotificationArea;
```

---

#### Задача 3: Сборка в App (Интеграция с useState)

Теперь соединим всё в `App.jsx` и добавим интерактивности, используя знания из прошлых уроков (`useState` из урока 1.10).

**JS (Скелет `src/App.jsx`):**

```jsx
import { useState } from "react";
import "./App.css";
// Импортируй созданные компоненты: UserStatus и NotificationArea

function App() {
  // Состояние для статуса (true/false)
  const [isOnline, setIsOnline] = useState(false);
  // Состояние для количества сообщений (число)
  const [messages, setMessages] = useState(0);

  return (
    <div className="app-container">
      <h1>Условный рендеринг</h1>

      <section>
        <h2>Задача 1: Статус</h2>
        {/* Рендери UserStatus, передай isOnline */}

        <button onClick={() => setIsOnline(!isOnline)}>
          Переключить статус (Toggle)
        </button>
      </section>

      <hr />

      <section>
        <h2>Задача 2: Уведомления</h2>
        {/* Рендери NotificationArea, передай messages как newMessagesCount */}

        <div style={{ display: "flex", gap: "10px" }}>
          <button onClick={() => setMessages(messages + 1)}>
            Добавить сообщение
          </button>
          <button onClick={() => setMessages(0)}>Очистить</button>
        </div>
      </section>
    </div>
  );
}

export default App;
```

**CSS (добавь в `src/App.css` для контейнера):**

```css
.app-container {
  max-width: 600px;
  margin: 0 auto;
  font-family: sans-serif;
  padding: 20px;
}
button {
  margin-top: 10px;
  padding: 8px 12px;
  cursor: pointer;
}
hr {
  margin: 20px 0;
  border: 0;
  border-top: 1px solid #ccc;
}
```

---

### ✅ Проверка результата

1.  Запусти проект (`npm run dev`).
2.  **Задача 1:** Нажми "Переключить статус". Плашка должна менять цвет (зеленый/серый) и текст ("В сети"/"Не в сети").
3.  **Задача 2:**
    - При `0` сообщений блок уведомлений **скрыт** (цифра 0 не должна висеть на экране!).
    - Нажми "Добавить сообщение" — блок должен появиться.
    - Нажми "Очистить" — блок должен исчезнуть.

Жду, когда реализуешь код! Если возникнут вопросы по синтаксису `{ condition && <Component /> }`, спрашивай.
