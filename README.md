Привет! Я твой ментор по React. Мы переходим к одной из самых фундаментальных тем — управлению состоянием. Без понимания этого урока создавать интерактивные интерфейсы невозможно.

В проекте `my-app-2026` мы сейчас находимся на этапе: **Урок 1.10: Хук useState**.

Ниже представлена подробная теория, основанная на транскрипте видеоурока и программе курса, а затем — практические задачи для закрепления.

---

### 📘 ТЕОРИЯ: Хук useState и управление состоянием

#### 1. Что такое состояние (State)?

В React состояние (`state`) — это объект, который хранит данные компонента. В отличие от обычных переменных, при изменении состояния React автоматически обновляет интерфейс (ре-рендер), делая его интерактивным.

- **State vs Props:** `Props` (свойства) приходят извне и неизменяемы (read-only). `State` — это внутренние данные, которыми компонент управляет сам.
- **Изоляция:** Каждый экземпляр компонента имеет свое собственное независимое состояние. Если вы отрендерите одну и ту же кнопку дважды, их счетчики будут работать раздельно.

#### 2. Синтаксис и подключение

Хуки (Hooks) — это специальные функции, позволяющие "подцепиться" к возможностям React. Они работают только внутри функциональных компонентов.

Для работы нужно импортировать хук:

```javascript
import { useState } from "react"; //
```

Инициализация состояния:

```javascript
const [count, setCount] = useState(0);
```

Разбор строки:

1.  **`useState(0)`**: Вызов хука. `0` — это начальное значение (может быть числом, строкой, булевым значением, объектом или массивом).
2.  **`[count, setCount]`**: Деструктуризация массива. Хук всегда возвращает массив из двух элементов:
    - `count` (имя произвольное): переменная с текущим значением.
    - `setCount` (принято добавлять префикс `set`): функция для обновления этого значения.

#### 3. Асинхронность и обновление (Важный нюанс!)

Функция обновления (например, `setCount`) не изменяет переменную мгновенно. Она "планирует" обновление интерфейса.

Если вызвать обновление несколько раз подряд, React может объединить их (batching) в один цикл рендеринга.
**Пример проблемы:**

```javascript
// Если count = 0
setCount(count + 1);
setCount(count + 1);
setCount(count + 1);
// В итоге count станет 1, а не 3, так как во всех вызовах берется старое значение count.
```

**Решение (Functional Update):**
Чтобы гарантировать работу с _актуальным_ состоянием, передавайте в `setCount` **колбэк-функцию**. Она принимает предыдущее значение (`prev`) как аргумент:

```javascript
setCount((prev) => prev + 1);
setCount((prev) => prev + 1);
setCount((prev) => prev + 1);
// Теперь count станет 3,.
```

#### 4. Подъем состояния (State Lifting)

Если двум соседним компонентам (например, `Header` и `Footer`) нужны общие данные, мы не можем передать их напрямую друг другу.
**Правило:** Состояние нужно поднять (переместить) в их ближайшего общего родителя (обычно `App`).

**Алгоритм:**

1.  Создать `state` в родителе.
2.  Передать само значение (`value`) в один компонент через пропсы (для чтения).
3.  Передать функцию изменения (`setValue`) в другой компонент через пропсы (для управления),.

---

### 🛠 ПРАКТИКА

В `App.jsx` у тебя сейчас пусто. Мы будем наполнять его компонентами.

#### Задача 1: Независимый счетчик

Создадим простой компонент, чтобы проверить изоляцию состояния.

**1. CSS:**
Вставь этот код в `src/index.css` (он будет глобальным для простоты):

```css
body {
  font-family: sans-serif;
  display: flex;
  justify-content: center;
  padding: 20px;
  background-color: #f0f0f0;
}
.card {
  background: white;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  margin: 10px;
  text-align: center;
}
.btn {
  background: #646cff;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 4px;
  cursor: pointer;
  margin-top: 10px;
}
.btn:hover {
  background: #535bf2;
}
```

**2. Компонент:**
Создай файл `src/components/SimpleCounter.jsx`.

```javascript
// TODO: Импортируй useState из react

const SimpleCounter = () => {
  // TODO: Инициализируй состояние count со значением 0

  const handleClick = () => {
    // TODO: Увеличь count на 1, используя функцию set...
  };

  return (
    <div className="card">
      {/* Выведи текущее значение count */}
      <h3>Счетчик: {/* ... */}</h3>
      <button className="btn" onClick={handleClick}>
        Нажми меня
      </button>
    </div>
  );
};

export default SimpleCounter;
```

**3. Подключение:**
В `src/App.jsx` импортируй `SimpleCounter` и отрендери его **дважды**, чтобы увидеть независимость состояний.

```javascript
import SimpleCounter from "./components/SimpleCounter";

function App() {
  return (
    <div>
      <h1>Урок 1.10: useState</h1>
      <div style={{ display: "flex" }}>
        <SimpleCounter />
        <SimpleCounter />
      </div>
    </div>
  );
}

export default App;
```

---

#### Задача 2: "Супер-счетчик" и проблема асинхронности

Реализуем кнопку, которая должна увеличивать значение на 3 за одно нажатие, используя три вызова функции обновления.

**1. Компонент:**
Создай файл `src/components/BatchCounter.jsx`.

```javascript
import { useState } from "react";

const BatchCounter = () => {
  const [score, setScore] = useState(0);

  const handleTripleClick = () => {
    // ЗАДАЧА: Напиши здесь код, который увеличивает score на 3.
    // Сначала попробуй написать setScore(score + 1) три раза подряд и посмотри, что будет.
    // Затем исправь это, используя callback-функцию: setScore(prev => ...).
    // 1. Неправильный вариант (раскомментируй для теста):
    // setScore(score + 1);
    // setScore(score + 1);
    // setScore(score + 1);
    // 2. Правильный вариант (напиши сам):
    // ...
  };

  return (
    <div className="card">
      <h3>Супер-счетчик: {score}</h3>
      <button className="btn" onClick={handleTripleClick}>
        +3 Очка
      </button>
    </div>
  );
};

export default BatchCounter;
```

**2. Подключение:**
Добавь `<BatchCounter />` в `App.jsx`.

---

#### Задача 3: Подъем состояния (Переключатель темы)

Реализуем смену темы (светлая/темная) для всего приложения. Состояние будет жить в `App`, а переключатель — в отдельном компоненте,.

**1. CSS (дополни в index.css):**

```css
.dark-mode {
  background-color: #333;
  color: white;
  min-height: 100vh; /* На весь экран */
  padding: 20px;
}
.light-mode {
  background-color: #f0f0f0;
  color: black;
  min-height: 100vh;
  padding: 20px;
}
```

**2. Компонент переключателя:**
Создай файл `src/components/ThemeToggle.jsx`.
Он должен принимать пропс для изменения темы, но сам не должен хранить состояние "темноты".

```javascript
// Обрати внимание: компонент принимает props
const ThemeToggle = ({ toggleTheme, isDark }) => {
  return (
    <div className="card" style={{ color: "black" }}>
      {" "}
      {/* card всегда белый для контраста */}
      <p>Текущая тема: {isDark ? "Темная 🌙" : "Светлая ☀️"}</p>
      {/* При клике вызываем функцию, пришедшую из родителя */}
      <button className="btn" onClick={toggleTheme}>
        Переключить тему
      </button>
    </div>
  );
};

export default ThemeToggle;
```

**3. Модификация App.jsx (Родитель):**
Теперь перепиши `App.jsx`, чтобы управлять классом обертки.

```javascript
import { useState } from 'react';
import SimpleCounter from './components/SimpleCounter';
import BatchCounter from './components/BatchCounter';
import ThemeToggle from './components/ThemeToggle';

function App() {
  // TODO: Создай состояние isDark (boolean), по умолчанию false

  // Функция для переключения (можно передать её, или использовать inline в пропсе)
  const handleToggle = () => {
    // TODO: Инвертируй значение isDark (true -> false, false -> true)
  };

  return (
    // Применяем класс динамически в зависимости от стейта
    <div className={/* TODO: если isDark true -> 'dark-mode', иначе 'light-mode' */ "light-mode"}>
      <h1>Управление состоянием</h1>

      {/* Передаем состояние и функцию управления вниз */}
      <ThemeToggle toggleTheme={handleToggle} isDark={/* передай стейт */} />

      <div style={{ display: 'flex', flexWrap: 'wrap' }}>
        <SimpleCounter />
        <BatchCounter />
      </div>
    </div>
  );
}

export default App;
```

### 🏁 Навигация и проверка

1.  **Задача 1:** Ты должен увидеть две карточки с кнопками. Клик по одной не меняет число на другой.
2.  **Задача 2:** Клик по "+3 Очка" должен увеличивать счетчик ровно на 3. Если увеличивает на 1 — ты забыл про колбэк `prev => ...`.
3.  **Задача 3:** Нажимая "Переключить тему", фон всего приложения должен меняться с серого на темный, а текст заголовка — на белый.

Выполняй по очереди. Если застрянешь — спрашивай!
